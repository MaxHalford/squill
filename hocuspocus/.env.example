# Must match JWT_SECRET in backend/.env exactly — used to verify user session tokens
# Generate with: openssl rand -hex 32
JWT_SECRET=

# PostgreSQL connection string (note: SQLite is not supported — pg driver required)
# Example: postgresql://user:password@localhost:5432/squill
DATABASE_URL=

# Port to listen on (default: 1234)
PORT=1234
