input:
  query: |
    SELECT *
    FROM orders
    ORDER BY created_at DESC
  instruction: "Add a WHERE clause for created_at > '2024-01-01'"
  database_dialect: "postgres"
expected:
  query: |
    SELECT *
    FROM orders
    WHERE created_at > '2024-01-01'
    ORDER BY created_at DESC
