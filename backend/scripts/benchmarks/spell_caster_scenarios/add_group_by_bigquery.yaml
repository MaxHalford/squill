input:
  query: |
    SELECT
      country,
      status,
      amount
    FROM orders
  instruction: "Group by country and status, and sum the amount"
  database_dialect: "bigquery"
expected:
  query: |
    SELECT
      country,
      status,
      SUM(amount) AS amount
    FROM orders
    GROUP BY country, status
