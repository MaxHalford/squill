input:
  query: |
    SELECT
      a.user_id,
      b.name
    FROM `project_a.analytics.events` a
    JOIN `project_a.core.users` b ON b.user_id = a.user_id
  instruction: "Replace the BigQuery project 'project_a' with 'project_b' in all table references"
  database_dialect: "bigquery"
expected:
  query: |
    SELECT
      a.user_id,
      b.name
    FROM `project_b.analytics.events` a
    JOIN `project_b.core.users` b ON b.user_id = a.user_id
