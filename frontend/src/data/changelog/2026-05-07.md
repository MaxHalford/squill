- Bring your own Google OAuth client for BigQuery sign-in. If your Google Workspace admin restricts third-party OAuth apps, configure your own client ID and secret from the Connection menu → BigQuery → settings cog, or via the `SQUILL_GOOGLE_CLIENT_ID` and `SQUILL_GOOGLE_CLIENT_SECRET` environment variables.
- The Connection menu now shows the active connection's database icon next to the label.
- Fixed the desktop build (`reqwest` dependency and `general_rate_limiter` field) so `cargo tauri build` works again.
