// Placeholder: native protocol clients (postgres wire, mysql, etc.) will
// live here. Currently empty — BigQuery uses the OAuth REST API directly
// from the JS webview (works in both browser and Tauri).
