name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install frontend dependencies
        run: cd frontend && bun install --frozen-lockfile

      - name: Install prek
        run: uv tool install prek

      - name: Run prek
        run: prek run --all-files

  frontend-tests:
    name: Frontend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: cd frontend && bun install --frozen-lockfile

      - name: Run tests
        run: cd frontend && bun run test:run

  deploy:
    needs: [lint, frontend-tests]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    container: ghcr.io/railwayapp/cli:latest
    strategy:
      matrix:
        include:
          - name: backend
            service_id: f6001fcc-4b46-45e6-9a74-73f5fbca10e2
          - name: frontend
            service_id: 7293644f-9a89-49fb-be39-6fe80c8ae97b
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Deploy ${{ matrix.name }} to Railway
        run: railway up --service=${{ matrix.service_id }} --detach
