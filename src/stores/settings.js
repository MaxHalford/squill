import { defineStore } from 'pinia'

export const useSettingsStore = defineStore('settings', () => {
  // Settings store can be used for future settings if needed
  // Font settings have been removed - using opinionated defaults instead

  return {}
})
